# Field

Filters above a table: `.vx-select` on surface, `.vx-input` search on surface-sunken that turns white with a navy border on focus.

- Provide: selects whose first option names the unfiltered state ("Toutes les familles", "Prix toutes zones"); a search placeholder "Rechercher un <objet>…".
- Group them in `.vx-filters` (10px gap, wraps). 14px text, radius-9, 10px vertical padding.
- Filtering is instant; no Apply button.
