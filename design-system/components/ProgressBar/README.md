# ProgressBar

A labelled 7px rail showing a share (poids d'un client, CA par activité, top clients du mois).

- Provide: name, value text (share · amount), and a fill width relative to the largest item.
- Fill is navy; `--alert` (negative) when the item crosses the configured threshold (dependency seuil).
- Use 6px rails inside denser month lists.
