# Badge

Pill tag for a line's type in a table: navy on navy-tint for the default, `--warn` (warning on warning-bg) for travaux supplémentaires.

- Provide: 2–4 characters or one word. Never decorative: a badge always encodes a type the reader filters on.
